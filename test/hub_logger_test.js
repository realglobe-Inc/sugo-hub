/**
 * Test case for hubLogger.
 * Runs with mocha.
 */
'use strict'

const hubLogger = require('../lib/logging/hub_logger.js')
const assert = require('assert')

describe('hub-logger', function () {
  this.timeout(3000)

  before(() => {

  })

  after(() => {

  })

  it('Hub logger', () => {

  })
})

/* global describe, before, after, it */
