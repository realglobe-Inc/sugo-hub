/**
 * Test case for ioInterceptor.
 * Runs with mocha.
 */
'use strict'

const ioInterceptor = require('../lib/helpers/io_interceptor.js')
const assert = require('assert')


describe('io-interceptor', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Io interceptor', async () => {

  })
})

/* global describe, before, after, it */
