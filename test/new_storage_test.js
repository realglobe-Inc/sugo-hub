/**
 * Test case for newStorage.
 * Runs with mocha.
 */
'use strict'

const newStorage = require('../lib/helpers/new_storage.js')
const assert = require('assert')


describe('new-storage', () => {
  before(async () => {

  })

  after(async () => {

  })

  it('New storage', async () => {
    assert.ok(newStorage)
  })
})

/* global describe, before, after, it */
