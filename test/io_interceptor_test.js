/**
 * Test case for ioInterceptor.
 * Runs with mocha.
 */
'use strict'

const ioInterceptor = require('../lib/helpers/io_interceptor.js')
const assert = require('assert')
const co = require('co')

describe('io-interceptor', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Io interceptor', () => co(function * () {

  }))
})

/* global describe, before, after, it */
