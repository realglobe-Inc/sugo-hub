/**
 * Test case for clusterMixin.
 * Runs with mocha.
 */
'use strict'

const clusterMixin = require('../lib/mixins/cluster_mixin.js')
const assert = require('assert')

describe('cluster-mixin', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Cluster mixin', async () => {

  })
})

/* global describe, before, after, it */
